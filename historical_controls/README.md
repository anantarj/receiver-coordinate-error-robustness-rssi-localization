# Historical controls, not new experiments

The frozen log `../results/base_logs/diffrepair_e2b_k14_dir8.txt` includes Block21 variants1–3. These observations were described in earlier benchmark drafts. They remain attributed exploratory history; current Paper A R5.1 is not represented as re-reporting the old three cross-sections.

`fraction_5km_saved_summary.csv` and `high_low_saved_summary.csv` extract the log at its original precision. `CONFIGURATION_REGISTER.json` reconstructs58 target/direction designs using the exact source seeds and numeric PHYS iteration order; it does not generate localization outcomes. `EXPOSURE_IDENTITY_CHECK.json` verifies the original5712/169 selected-reception counts using the pinned CSV and stored B population. `SOURCE_STATUS.json` binds the log and interpretation.

The single-field227.9% high-exposure point is not the later eight-field233.4% central result. Four-direction pooled percentiles and two-field refit/freeze ablation are also different summaries. Legacy fitted-field variant2 is retained as motivation only; it is not regenerated from the resource audit's newer stable fitted map, and no missing old environment, fitted artifact or per-case error array is fabricated. Main spatial claims use the named current perturbation families and reproduced controls.

The source's historical narrative labels (`true map`, `repair`, or exclusive attribution to receiver identity) do not supersede the current article's conditional interpretations. Shared observations are not separate new contributions merely because a related manuscript remains unsubmitted.
